Se declaran dos variables de tipo flotante.
float almacena números con decimales.
Cada variable ocupa normalmente 4 bytes en memoria.
Se crea un arreglo de 24 elementos tipo float.
Un arreglo es una colección de variables del mismo tipo almacenadas consecutivamente en memoria.
Cada variable tiene una posición única en la memoria RAM.
